# SkillMind 使用说明

> 🧠 全源知识提炼系统 v2.2 — 将 `SKILL.md`、博客、论坛、网页经验提炼为 Obsidian 知识库

---

## 安装

### 基础安装

```bash
pip install -e .
```

### 可选功能安装

```bash
# 语义搜索（需要 chromadb）
pip install "skillmind[search]"


# RSS 解析增强（feedparser，降级时用 httpx+正则）
pip install "skillmind[rss]"


# Web 正文提取增强（crawl4ai，降级时用 httpx+正则）
pip install "skillmind[web]"


# 一次安装所有可选功能
pip install "skillmind[full]"
```

安装完成后即可在终端使用 `skillmind` 命令。

---

## 命令总览

| 命令 | 说明 |
|------|------|
| `skillmind ingest skill <path\|url>` | 采集本地目录或 Git 仓库中的 SKILL.md |
| `skillmind ingest rss <feed_url>` | 订阅博客 RSS/Atom Feed，批量抓取文章 |
| `skillmind ingest url <article_url>` | 抓取单篇文章 / 网页正文 |
| `skillmind ingest forum <topic_url>` | 采集论坛主题帖（Discourse / Reddit / HN） |
| `skillmind extract` | 对缓存内容执行 LLM 知识提取，生成草稿（支持一文多卡） |
| `skillmind review` | 列出草稿，展示来源类型与可信度 |
| `skillmind edit <uuid>` | 使用编辑器手动修改某个草稿 |
| `skillmind publish <uuid\|--all>` | 将草稿发布到 Obsidian Vault |
| `skillmind trace <uuid>` | 溯源：打开笔记对应的原始网页或本地文件 |
| `skillmind search "<query>"` | 在知识库中进行语义搜索（需安装 chromadb） |
| `skillmind sync` | 重新扫描 Vault，更新向量索引 |
| `skillmind status` | 展示知识库统计信息 |
| `skillmind update --check` | 检查上游 Skill 仓库是否有更新 |
| `skillmind config` | 查看或修改 SkillMind 配置（含 LLM 认证） |

---

## 命令详细说明

### `ingest skill` — 采集 SKILL.md

采集本地目录或远程 Git 仓库中的 `SKILL.md` 文件，写入本地缓存区（`~/.skillmind/cache/`）。
相同内容（SHA256 哈希一致）自动跳过，不重复导入。

**支持的来源格式：**
- 本地路径：`./my-skills-folder`
- 仓库根地址：`https://github.com/owner/repo`
- GitHub 子目录：`https://github.com/owner/repo/tree/main/skills/xxx`（自动只扫描该子目录）

```bash
# 采集本地目录（递归扫描所有 SKILL.md / *.skill.md）
skillmind ingest skill ./my-skills-folder

# 采集 Git 仓库（自动浅克隆，depth=1）
skillmind ingest skill https://github.com/anthropics/claude-code-skills

# 采集 GitHub 子目录
skillmind ingest skill https://github.com/xstongxue/best-skills/tree/main/skills/frontend
```

> 💡 **Windows 路径**：本地路径建议使用正斜杠或引号包裹，例如 `skillmind ingest skill "D:\my-skills"`

---

### `ingest rss` — 订阅 RSS Feed

批量订阅博客 RSS/Atom Feed，自动抓取每篇文章正文并缓存（`doc_type=blog`）。
优先使用 `feedparser`（需 `pip install feedparser`），降级用 httpx + 正则解析。

```bash
# 订阅博客 RSS（默认最多抓取 50 篇）
skillmind ingest rss https://blog.example.com/feed

# 限制抓取数量
skillmind ingest rss https://blog.example.com/feed --max 10
```

**选项说明：**

| 选项 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--max <n>` | `-n` | `50` | 最多抓取条目数 |

---

### `ingest url` — 抓取单篇文章

抓取单篇网页/博客文章正文，自动清洗 HTML 为 Markdown（`doc_type=blog`）。
优先使用 `crawl4ai`（需 `pip install crawl4ai`），降级用 httpx。

```bash
skillmind ingest url https://martinfowler.com/articles/patterns-of-distributed-systems
```

---

### `ingest forum` — 采集论坛帖子

采集论坛主题帖，自动聚合主帖与所有回复（`doc_type=forum_post`）。
支持 Discourse 站点（优先走 JSON API 获取完整内容），其他站点降级为通用网页抓取。

```bash
# Discourse 论坛（自动走 API）
skillmind ingest forum https://community.example.com/t/my-topic/12345

# Reddit / HN / 其他论坛（通用抓取）
skillmind ingest forum https://news.ycombinator.com/item?id=12345678
```

---

### `extract` — 知识提取

调用 LLM 对缓存区中的文档执行结构化知识提取，结果以草稿（`draft`）状态保存至 `~/.skillmind/drafts/`。

**v2.3 (W1 + W2.1) 提取流水线**：

```
原文 → chunker（语义切分 + 代码块保护）
      → identity 阶段：判定 1 或 N 张卡 + 身份信息
      → structure 阶段：对每张卡抽取 procedure / decisions / halt / rollback
      → enhancement 阶段：补 plain_summary / 可信度 / 过时风险
      → 合并 → 草稿落盘
```

**核心特性：**

- **3 阶段提取**：megaprompt 拆分，单 prompt 复杂度降低，每阶段独立 schema 校验
- **chunker 切分**：长文不再被截到 6000 字符，默认预算 30000，超长按整 chunk 边界截尾
- **一文多卡**：identity 阶段决定卡片数量，后续阶段输出 N 个 units 对齐
- **JSON Schema 校验 + 反馈重试**：LLM 输出字段缺失/类型错乱时，把错误反馈给 LLM 一次重试（仅 1 次，避免死循环）
- **阶段独立缓存**：缓存 key = `(source_hash, prompt_version, stage)`，改任一阶段的 prompt 只需重跑该阶段，其他阶段保留缓存
- **动态 Prompt 路由**：stage × doc_type 二维路由（如 identity-blog 偏多卡 / structure-forum 把多个回答塞进 decision_points）
- **可信度 / 时效性标注**：`source_reliability` 与 `obsolescence_risk` 由 enhancement 阶段输出

```bash
# 提取全部缓存文件
skillmind extract

# 只提取指定类型（skill / blog / forum_post / webpage）
skillmind extract --type blog

# 按路径关键词或 source_hash 前缀过滤
skillmind extract --skill pg-upgrade

# 提取后自动批准（信任来源时使用，跳过人工审核）
skillmind extract --auto-approve
```

**选项说明：**

| 选项 | 简写 | 说明 |
|------|------|------|
| `--skill <keyword>` | `-s` | 按路径或哈希前缀筛选 |
| `--type <doc_type>` | `-t` | 只处理指定类型：`skill` / `blog` / `forum_post` / `webpage` |
| `--auto-approve` | — | 提取后自动将草稿状态置为 `approved` |

**典型日志输出**：

```
切分: 4 chunks, ≈3466 tokens, 送入 LLM 10860 字符
identity: 2 卡 → 零停机 PostgreSQL 升级, 复制槽监控
structure: 阶段缓存命中
enhancement: ✓ 反馈重试后 schema 校验通过
```

**两层重试机制：**

1. **transport 层**（超时/网络错误/JSON 语法错误）：指数退避（1s、2s、4s），默认最多 2 次
2. **content 层**（schema 校验失败）：把错误反馈给 LLM，**仅重试 1 次**（避免死循环）
3. 任一阶段两层重试全部失败 → 整篇文档降级为**启发式规则提取**（基于 Markdown 区块分析）
4. **凭证缺失**（未配置 API Key）→ 直接报错，不静默降级，需先运行 `skillmind config`

**缓存目录结构**（v3 起按阶段切分）：

```
~/.skillmind/cache/extract_cache/
├── <hash>_extract_v3_identity.json
├── <hash>_extract_v3_structure.json
└── <hash>_extract_v3_enhancement.json
```

> 💡 **prompt_version v3 已强制升级**：W2.1 引入 3 阶段后 v2 旧缓存自然失效（key 不匹配），无需手动清理。旧 `<hash>_extract_v1.json` / `<hash>_extract_v2.json` 文件可直接删除：`rm ~/.skillmind/cache/extract_cache/*_extract_v[12]*.json`

---

### `review` — 草稿审核列表

以表格形式列出草稿，显示来源类型（skill/blog/forum_post）、可信度、时效性风险、状态等。

```bash
# 列出所有待审核（draft）草稿（默认）
skillmind review

# 列出已批准待发布草稿
skillmind review --status approved

# 列出所有已发布笔记
skillmind review --status published

# 列出全部（不过滤状态）
skillmind review --status all
```

**可信度说明（`source_reliability`）：**

| 标识 | 含义 |
|------|------|
| 🟢 high | 官方文档、知名团队 Skill 仓库 |
| 🟡 medium | 大型技术博客、活跃维护的开源项目 |
| 🔴 low | 个人博客、论坛回答（建议交叉验证） |

**时效性说明（`obsolescence_risk`）：**

| 标识 | 含义 |
|------|------|
| 🟢 low | 概念解释、设计原则（较稳定） |
| 🟡 medium | 部署流程、工具用法（可能过时） |
| 🔴 high | 具体版本号、API 细节（请核实版本） |

---

### `edit` — 编辑草稿

使用系统默认编辑器打开指定草稿的 JSON 文件，支持 UUID 前缀匹配。

```bash
skillmind edit skill-8f3a1b2

# 支持前缀匹配，无需完整 UUID
skillmind edit 8f3a
```

> 💡 编辑器优先级：`SKILLMIND_EDITOR` 环境变量 → `config.yaml` 中的 `editor` 字段 → `EDITOR` 环境变量 → 平台默认（Windows: 记事本，macOS: TextEdit，Linux: nano）

---

### `publish` — 发布到 Vault

将草稿渲染为 Obsidian 兼容的 Markdown 笔记，写入 Vault（`~/.skillmind/vault/skills/`）。

发布后的笔记包含完整 YAML front matter（uuid、tags、doc_type、source_reliability、obsolescence_risk 等），兼容 Obsidian Dataview 插件：

```dataview
LIST FROM #migration WHERE draft = false SORT file.ctime DESC
```

```bash
# 发布单个草稿
skillmind publish skill-8f3a1b2

# 批量发布所有 draft / approved 状态草稿
skillmind publish --all
```

**一文多卡笔记**会自动标注序号和父文档链接，例如：`笔记标题（2/5）`。

---

### `trace` — 溯源

打开笔记对应的原始来源（网页或本地文件），便于核实内容或查看原文。

```bash
skillmind trace skill-8f3a1b2
```

- 博客/论坛/网页来源 → 在浏览器打开原始 URL
- Git 仓库来源 → 在浏览器打开 GitHub blob 链接（精确到文件）
- 本地文件来源 → 用系统默认程序打开本地文件

---

### `search` — 语义搜索

在知识库中进行自然语言语义搜索（需安装 `chromadb`）。

```bash
# 安装依赖
pip install "skillmind[search]"


# 搜索
skillmind search "零停机数据库升级"
skillmind search "如何回滚 Docker 容器"

# 控制返回结果数量（默认 5）
skillmind search "postgresql migration" --top-k 10
```

| 选项 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--top-k <n>` | `-k` | `5` | 返回结果数量 |

---

### `sync` — 重建向量索引

重新扫描 Vault 中所有已发布的 Markdown 笔记，全量更新 Chroma 向量索引。
手动在 Obsidian 中修改笔记后，运行此命令保证搜索时效性。

```bash
skillmind sync
```

---

### `status` — 知识库状态

展示统计概览：缓存文件数（按类型细分）、各状态草稿数、旧版 Prompt 笔记数等。

```bash
skillmind status
```

输出示例：

```
┌──────────────────────────────────────────────────────┐
│                  SkillMind 状态概览                  │
├────────────────────────────────┬─────────────────────┤
│ 已缓存原始文件                  │ 57                  │
│   └ skill                      │ 30                  │
│   └ blog                       │ 20                  │
│   └ forum_post                 │ 7                   │
│ 草稿（待审核）                  │ 3                   │
│ 已批准（待发布）                │ 1                   │
│ 已发布                         │ 53                  │
│ 旧版 Prompt 笔记               │ 0                   │
│ Vault 路径                     │ ~/.skillmind/vault  │
│ 当前 Prompt 版本               │ extract_v3          │
│ LLM 模型                       │ anthropic/claude-3-5-haiku-20241022 │
└────────────────────────────────┴─────────────────────┘
```

---

### `update --check` — 检查上游更新

对通过 Git URL 采集的仓库，拉取远端最新 commit，提示是否需要重新提取。

```bash
skillmind update --check
```

若检测到上游有新 commit，建议：

```bash
skillmind ingest skill <repo_url>
skillmind extract
```

---

### `config` — 配置管理

查看或修改所有配置项。配置保存于 `~/.skillmind/config.yaml`。

#### 查看当前配置

```bash
skillmind config --show
```

#### 配置 LLM 认证

**方式一：按 Provider 分别存储 API Key（推荐）**

```bash
# Anthropic (Claude)
skillmind config --provider anthropic --api-key sk-ant-xxxxxxxx

# OpenAI
skillmind config --provider openai --api-key sk-xxxxxxxx

# DeepSeek
skillmind config --provider deepseek --api-key sk-xxxxxxxx

# Gemini
skillmind config --provider gemini --api-key AIzaxxxxxxxx

# Groq
skillmind config --provider groq --api-key gsk_xxxxxxxx

# 自定义 API Base（兼容第三方转发服务）
skillmind config --provider openai --api-key sk-xxx --api-base https://api.deepseek.com
```

切换模型时自动使用对应 Key：

```bash
skillmind config --model anthropic/claude-opus-4-5
skillmind config --model openai/gpt-4o
skillmind config --model deepseek/deepseek-chat
```

查看所有已配置 Key（自动脱敏）：

```bash
skillmind config --list-providers
```

**方式二：从环境变量读取（Key 不落盘）**

```bash
skillmind config --auth api_key --api-key-env ANTHROPIC_API_KEY
```

系统自动识别以下环境变量，无需额外配置：

| 环境变量 | Provider |
|----------|----------|
| `ANTHROPIC_API_KEY` | Anthropic (Claude) |
| `OPENAI_API_KEY` | OpenAI |
| `DEEPSEEK_API_KEY` | DeepSeek |
| `GEMINI_API_KEY` | Google Gemini |
| `GROQ_API_KEY` | Groq |
| `MISTRAL_API_KEY` | Mistral |
| `OPENROUTER_API_KEY` | OpenRouter |

**方式三：Claude Code Max 订阅（免 API Key）**

复用 Claude Code 本地 OAuth 凭证，不消耗 API 额度：

```bash
claude login   # 前提：已安装并登录 Claude Code
skillmind config --auth claude_code_max
```

**Key 查找优先级：**

1. `llm.api_key`（配置文件直接写入）
2. `llm.api_key_env` 指定的环境变量
3. `api_keys.<provider>` 多 Provider 存储区
4. 根据 model 前缀自动匹配对应环境变量
5. 遍历所有已知 Provider 环境变量兜底

#### 测试连接

```bash
skillmind config --test
```

#### 其他配置项

```bash
# Obsidian Vault 目录
skillmind config --vault "D:\Markdown\my-vault"

# 每分钟 LLM 请求数（防触发速率限制）
skillmind config --qpm 5
```

高级配置（直接编辑 `~/.skillmind/config.yaml`）：

```yaml
llm:
  max_content_chars: 30000  # 单次 LLM 调用送入的正文字符上限（W1 后默认 30000）
  timeout: 120              # 单次 LLM 调用超时秒数（默认 120）
  max_retries: 2            # transport 层失败重试次数，指数退避（默认 2）
  claude_code_api_base: https://api.anthropic.com  # Claude Code Max 模式 API 地址

# Chunker 配置（W1 新增）：长文切分为 LLM 友好的语义片段
chunker:
  chunk_size_tokens: 1500       # 单 chunk 目标 token 数
  chunk_overlap_tokens: 150     # 相邻 chunk 重叠 token 数
  chars_per_token: 3            # token 估算系数（混合中英文取 3，纯英文文档可改 4）
  min_chunk_size_tokens: 100    # chunk 最小尺寸

# 草稿编辑器（skillmind edit 使用）
editor: code   # 例如：code（VS Code）、vim、subl、notepad
```

也可通过环境变量临时指定编辑器：

```bash
# Windows PowerShell
$env:SKILLMIND_EDITOR = "code"; skillmind edit skill-8f3a1b2

# Linux / macOS
SKILLMIND_EDITOR=vim skillmind edit skill-8f3a1b2
```

**`config` 完整选项说明：**

| 选项 | 说明 |
|------|------|
| `--auth <mode>` | 认证模式：`api_key` 或 `claude_code_max` |
| `--provider <name>` | 指定 Provider，如 `anthropic` / `openai` / `deepseek` |
| `--api-key <key>` | 直接设置 API Key |
| `--api-key-env <var>` | 从环境变量读取 Key |
| `--api-base <url>` | 自定义 API 地址 |
| `--model <model>` | LLM 模型，格式 `provider/model-name` |
| `--vault <path>` | Obsidian Vault 目录路径 |
| `--qpm <n>` | 每分钟最大 LLM 请求数 |
| `--show` | 查看当前所有配置（Key 自动脱敏） |
| `--list-providers` | 列出所有已配置的 Provider 及 Key 状态 |
| `--test` | 发送测试请求，验证 LLM 连接 |

---

## 典型工作流

### 首次构建知识库

```bash
# 1. 配置 LLM
skillmind config --provider anthropic --api-key sk-ant-xxxxxxxx

# 2-A. 采集 SKILL.md 仓库
skillmind ingest skill https://github.com/anthropics/claude-code-skills

# 2-B. 同时订阅博客 RSS
skillmind ingest rss https://blog.example.com/feed --max 20

# 2-C. 采集单篇文章
skillmind ingest url https://martinfowler.com/articles/strangler-fig.html

# 3. 提取全部（LLM + 一文多卡）
skillmind extract

# 4. 审核草稿（查看可信度 / 时效性）
skillmind review

# 5. 溯源核实低可信度内容
skillmind trace <uuid>

# 6. 编辑不满意的草稿
skillmind edit <uuid>

# 7. 批量发布到 Obsidian
skillmind publish --all

# 8. 语义搜索
skillmind search "零停机升级"
```

### 增量更新

```bash
# 检查 Git 仓库是否有更新
skillmind update --check

# 重新采集（已有内容自动跳过）
skillmind ingest skill https://github.com/anthropics/claude-code-skills

# 只提取新内容（缓存命中自动跳过）
skillmind extract

# 查看新增草稿
skillmind review
skillmind publish --all
```

### 只采集博客不采集 Skill

```bash
skillmind ingest rss https://feeds.feedburner.com/martinfowler/bliki
skillmind extract --type blog
skillmind review
skillmind publish --all
```

---

## 数据存储位置

| 路径 | 内容 |
|------|------|
| `~/.skillmind/config.yaml` | 全局配置文件 |
| `~/.skillmind/hashes.yaml` | 文件哈希记录（用于去重） |
| `~/.skillmind/cache/raw/` | 采集的原始文件（按 SHA256 前两位分目录） |
| `~/.skillmind/cache/repos/` | 克隆的 Git 仓库 |
| `~/.skillmind/cache/extract_cache/` | LLM 提取结果缓存（避免重复消耗 Token） |
| `~/.skillmind/drafts/` | 草稿 JSON 文件 |
| `~/.skillmind/vault/skills/` | 已发布的 Obsidian Markdown 笔记 |
| `~/.skillmind/chroma/` | 向量索引数据库（chromadb） |

> 💡 所有数据均存储在本地，不上传任何云服务。可通过 `SKILLMIND_HOME` 环境变量自定义根目录。

---

## 支持的 LLM 模型

通过 [litellm](https://github.com/BerriAI/litellm) 统一调用，支持所有兼容接口：

| Provider | 模型示例 | 配置命令 |
|----------|---------|---------|
| Anthropic | `anthropic/claude-3-5-haiku-20241022`、`anthropic/claude-opus-4-5` | `--provider anthropic --api-key sk-ant-xxx` |
| OpenAI | `openai/gpt-4o`、`openai/gpt-4o-mini` | `--provider openai --api-key sk-xxx` |
| DeepSeek | `deepseek/deepseek-chat` | `--provider deepseek --api-key sk-xxx` |
| Google Gemini | `gemini/gemini-1.5-pro` | `--provider gemini --api-key AIza-xxx` |
| Groq | `groq/llama-3.3-70b-versatile` | `--provider groq --api-key gsk_xxx` |
| Mistral | `mistral/mistral-large-latest` | `--provider mistral --api-key xxx` |
| OpenRouter | `openrouter/anthropic/claude-3.5-sonnet` | `--provider openrouter --api-key xxx` |
| 本地模型 | `ollama/llama3` | `--provider openai --api-key ollama --api-base http://localhost:11434` |
| Claude Code Max | 免 Key，复用 Claude Code 订阅 | `--auth claude_code_max` |

---

## 故障排查

### `未配置 LLM API Key`

```bash
# 检查当前配置
skillmind config --show

# 配置 Key
skillmind config --provider anthropic --api-key sk-ant-xxx

# 测试连接
skillmind config --test
```

### `缓存文件不存在，请重新 ingest`

缓存目录被清理或手动删除了原始文件，重新采集即可：

```bash
skillmind ingest skill <path_or_url>
```

### `LLM 提取失败，降级启发式`

- 检查网络和 API Key 是否有效：`skillmind config --test`
- 调整超时时间：编辑 `~/.skillmind/config.yaml`，设置 `llm.timeout: 180`
- 调整限速：`skillmind config --qpm 3`

### `语义搜索需要安装 chromadb`

```bash
pip install "skillmind[search]"
skillmind sync   # 重建索引
```

### `RSS 未解析到任何条目`

```bash
# 安装 feedparser 增强解析
pip install feedparser

# 再次尝试
skillmind ingest rss <feed_url>
```

### Windows 路径问题

PowerShell 中路径包含空格时请用引号包裹，且用英文分号分隔命令：

```powershell
skillmind config --vault "D:\My Notes\Obsidian"
skillmind ingest skill "D:\Projects\my skills"
```
