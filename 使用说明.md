# SkillMind 使用说明

> 🧠 开源 Claude Code Skill 知识提炼系统 — 将分散的 `SKILL.md` 提炼为 Obsidian 知识库

---

## 安装

```bash
pip install -e .
```

安装完成后即可在终端使用 `skillmind` 命令。

---

## 命令总览

| 命令 | 说明 |
|------|------|
| `skillmind ingest <path\|url>` | 将本地目录或 Git 仓库中的 Skill 文件导入缓存区 |
| `skillmind extract` | 对缓存内的 Skill 执行知识提取，生成草稿 |
| `skillmind review` | 列出所有待审核的提取草稿 |
| `skillmind edit <uuid>` | 使用编辑器手动修改某个草稿 |
| `skillmind publish <uuid\|--all>` | 将草稿发布到 Obsidian Vault |
| `skillmind search "<query>"` | 在知识库中进行语义搜索（需启用 Chroma） |
| `skillmind sync` | 重新扫描 Vault，更新向量索引 |
| `skillmind status` | 展示知识库统计信息与待更新提示 |
| `skillmind update --check` | 检查上游 Skill 仓库是否有更新 |
| `skillmind config` | 查看或修改 SkillMind 配置（含 LLM 认证） |

---

## 命令详细说明

### `ingest` — 采集 Skill 文件

将本地目录或远程 Git 仓库中的 `SKILL.md` 文件导入到本地缓存区（`~/.skillmind/cache/`）。
相同内容（SHA256 哈希一致）的文件会自动跳过，不会重复导入。

**支持的 URL 格式：**
- 仓库根地址：`https://github.com/owner/repo`
- GitHub 子目录：`https://github.com/owner/repo/tree/main/skills/my-skill`（自动提取仓库地址 + 只扫描该子目录）
- 本地路径：`./my-skills-folder`

```bash
# 采集本地目录（递归扫描所有 SKILL.md / *.skill.md）
skillmind ingest ./my-skills-folder

# 采集 Git 仓库根目录（自动浅克隆）
skillmind ingest https://github.com/anthropics/skills

# 采集 GitHub 子目录（自动解析，只扫描该目录）
skillmind ingest https://github.com/xstongxue/best-skills/tree/main/skills/frontend-design
```

---

### `extract` — 知识提取

调用 LLM 对缓存区中的 Skill 文件进行结构化知识提取，结果以草稿（`draft`）状态保存至 `~/.skillmind/drafts/`。
提取结果包含：技能类型、执行流程、决策树、前置条件、中止条件、回滚方案、学习增强字段等。

```bash
# 提取全部缓存文件
skillmind extract

# 提取指定文件（按 source_hash 前缀或路径关键词过滤）
skillmind extract --skill pg-upgrade

# 提取后自动批准（信任来源时使用，跳过人工审核）
skillmind extract --auto-approve
```

**选项说明：**

| 选项 | 简写 | 说明 |
|------|------|------|
| `--skill <keyword>` | `-s` | 按路径或哈希前缀筛选要提取的文件 |
| `--auto-approve` | — | 提取后自动将草稿状态置为 `approved` |

---

### `review` — 草稿审核列表

以表格形式列出所有提取草稿，显示 UUID、名称、类型、状态、来源路径和创建日期。

```bash
# 列出所有 draft（待审核）状态的草稿（默认）
skillmind review

# 列出所有已发布的技能
skillmind review --status published

# 列出全部草稿（不过滤状态）
skillmind review --status all
```

**选项说明：**

| 选项 | 默认值 | 说明 |
|------|--------|------|
| `--status` | `draft` | 筛选状态：`draft` / `published` / `all` |

---

### `edit` — 编辑草稿

使用系统默认编辑器打开指定草稿的 JSON 文件，支持 UUID 前缀匹配。

```bash
# 编辑指定草稿（输入 UUID 或其前缀）
skillmind edit skill-8f3a1b2

# UUID 支持前缀匹配，无需输入完整 UUID
skillmind edit 8f3a
```

编辑并保存文件后，变更会自动写回草稿存储。可修改任意字段（如名称、标签、步骤描述等），保存时会自动进行 Schema 校验。

---

### `publish` — 发布到 Vault

将草稿渲染为 Obsidian 兼容的 Markdown 笔记，写入 Vault 正式目录（`~/.skillmind/vault/skills/`），并同步更新向量索引。

```bash
# 发布单个草稿
skillmind publish skill-8f3a1b2

# 批量发布所有 draft / approved 状态的草稿
skillmind publish --all
```

发布后的 Markdown 笔记包含完整的 YAML front matter，兼容 Obsidian Dataview 插件查询，例如：

```dataview
LIST FROM #migration WHERE draft = false SORT difficulty ASC
```

---

### `search` — 语义搜索

在知识库中进行自然语言语义搜索（需安装可选依赖 `chromadb`）。

```bash
# 安装语义搜索依赖
pip install "skillmind[search]"

# 搜索
skillmind search "零停机数据库升级"
skillmind search "如何回滚 Docker 容器"

# 控制返回结果数量（默认 5）
skillmind search "postgresql migration" --top-k 10
```

**选项说明：**

| 选项 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--top-k <n>` | `-k` | `5` | 返回结果数量 |

---

### `sync` — 重建向量索引

重新扫描 Vault 中所有已发布的 Markdown 笔记，全量更新 Chroma 向量索引。
当手动在 Obsidian 中修改了笔记内容后，运行此命令可保证搜索结果的时效性。

```bash
skillmind sync
```

---

### `status` — 知识库状态

展示当前知识库的统计概览，包括缓存文件数、各状态草稿数、已发布技能数、使用旧版 Prompt 的技能数等。

```bash
skillmind status
```

输出示例：

```
┌──────────────────────────────────────┐
│         SkillMind 状态概览           │
├────────────────────────┬─────────────┤
│ 已缓存原始文件          │ 42          │
│ 草稿（待审核）          │ 3           │
│ 已批准（待发布）        │ 1           │
│ 已发布                  │ 38          │
│ 使用旧版 Prompt 的技能  │ 0           │
│ Vault 路径              │ ~/.skillmind/vault │
│ 当前 Prompt 版本        │ extract_v1  │
│ LLM 模型                │ anthropic/claude-3-5-haiku-20241022 │
└────────────────────────┴─────────────┘
```

---

### `update --check` — 检查上游更新

对通过 Git URL 采集的仓库，拉取远端最新 commit，比对本地缓存版本，提示是否需要重新提取。

```bash
skillmind update --check
```

若检测到上游有新 commit，建议重新执行：

```bash
skillmind ingest <repo_url>
skillmind extract
```

---

### `config` — 配置管理

查看或修改 SkillMind 的所有配置项，包括 LLM 认证方式、模型选择、Vault 路径等。
配置保存于 `~/.skillmind/config.yaml`。

#### 查看当前配置

```bash
skillmind config --show
```

#### 配置 LLM 认证

**方式一：按 Provider 分别存储 API Key（推荐）**

支持同时存储多家 Provider 的 Key，切换模型时自动匹配对应 Key，互不干扰。

```bash
# Anthropic (Claude)
skillmind config --provider anthropic --api-key sk-ant-xxxxxxxx

# OpenAI
skillmind config --provider openai --api-key sk-xxxxxxxx

# DeepSeek
skillmind config --provider deepseek --api-key sk-xxxxxxxx

# Gemini
skillmind config --provider gemini --api-key AIzaxxxxxxxx

# Groq
skillmind config --provider groq --api-key gsk_xxxxxxxx

# 其他兼容 OpenAI 接口的服务（配合 --api-base）
skillmind config --provider openai --api-key sk-xxx --api-base https://api.deepseek.com
```

切换模型时自动使用对应 Provider 的 Key：

```bash
skillmind config --model anthropic/claude-opus-4-5     # 自动使用 anthropic key
skillmind config --model openai/gpt-4o                 # 自动使用 openai key
skillmind config --model deepseek/deepseek-chat        # 自动使用 deepseek key
```

查看所有已配置的 Key（自动脱敏显示）：

```bash
skillmind config --list-providers
```

**方式二：从环境变量读取（Key 不落盘）**

```bash
skillmind config --auth api_key --api-key-env ANTHROPIC_API_KEY
```

系统也会自动识别以下环境变量，无需额外配置：

| 环境变量 | Provider |
|----------|----------|
| `ANTHROPIC_API_KEY` | Anthropic (Claude) |
| `OPENAI_API_KEY` | OpenAI |
| `DEEPSEEK_API_KEY` | DeepSeek |
| `GEMINI_API_KEY` | Google Gemini |
| `GROQ_API_KEY` | Groq |
| `MISTRAL_API_KEY` | Mistral |
| `OPENROUTER_API_KEY` | OpenRouter |

**方式三：Claude Code Max 订阅认证（免 API Key）**

适合已订阅 Claude Code Max / Pro，复用 Claude Code 本地 OAuth 凭证，不消耗 API 额度。

```bash
# 前提：已安装并登录 Claude Code
claude login

# 切换到 Claude Code Max 模式
skillmind config --auth claude_code_max
```

**Key 查找优先级（api_key 模式）：**

1. `llm.api_key`（配置文件直接写入）
2. `llm.api_key_env` 指定的环境变量
3. `api_keys.<provider>` 多 Provider 存储区
4. 根据当前 model 前缀自动匹配对应环境变量
5. 依次遍历所有已知 Provider 环境变量兜底

#### 测试连接

```bash
skillmind config --test
```

#### 其他配置项

```bash
# 设置 Obsidian Vault 目录（关联已有的 Obsidian 库）
skillmind config --vault "D:\Markdown\my-vault"

# 设置 LLM 每分钟请求数限制（防止触发速率限制）
skillmind config --qpm 5
```

以下配置项需直接编辑 `~/.skillmind/config.yaml`：

```yaml
llm:
  # LLM 内容最大字符数（默认 6000），超长 Skill 可调大，避免截断
  max_content_chars: 8000

  # LLM 单次调用超时秒数（默认 120）
  # 防止 API 无响应时永久阻塞批量提取流程
  timeout: 120

  # LLM 失败重试次数（默认 2），使用指数退避（1s、2s）
  # 超过重试次数后才降级为启发式规则提取
  max_retries: 2

  # Claude Code Max 模式的 API 地址（默认 https://api.anthropic.com）
  # 企业代理或私有部署时修改
  claude_code_api_base: https://api.anthropic.com

# 草稿编辑器（skillmind edit 命令使用）
# 优先级：SKILLMIND_EDITOR 环境变量 > 此配置 > EDITOR 环境变量 > 平台默认
editor: code   # 例如：code（VS Code）、vim、subl
```

也可通过环境变量临时指定编辑器，无需修改配置文件：

```bash
# Windows PowerShell
$env:SKILLMIND_EDITOR = "code"
skillmind edit skill-8f3a1b2

# Linux / macOS
SKILLMIND_EDITOR=vim skillmind edit skill-8f3a1b2
```

**`config` 完整选项说明：**

| 选项 | 说明 |
|------|------|
| `--auth <mode>` | 认证模式：`api_key` 或 `claude_code_max` |
| `--provider <name>` | 指定 Provider 存储 Key，如 `anthropic` / `openai` / `deepseek` 等 |
| `--api-key <key>` | 直接设置 API Key（配合 `--provider` 可管理多个） |
| `--api-key-env <var>` | 从环境变量读取 Key，如 `ANTHROPIC_API_KEY` |
| `--api-base <url>` | 自定义 API 地址（兼容第三方服务） |
| `--model <model>` | LLM 模型，格式为 `provider/model-name` |
| `--vault <path>` | Obsidian Vault 目录路径 |
| `--qpm <n>` | 每分钟最大 LLM 请求数 |
| `--show` | 查看当前所有配置（Key 自动脱敏） |
| `--list-providers` | 列出所有已配置的 Provider 及 Key 状态 |
| `--test` | 发送测试请求，验证 LLM 连接 |

> 💡 `max_content_chars`、`claude_code_api_base`、`editor` 等高级配置项请直接编辑 `~/.skillmind/config.yaml`。

---

## 典型工作流

### 首次构建知识库

```bash
# 1. 配置 LLM
skillmind config --auth api_key --api-key-env ANTHROPIC_API_KEY

# 2. 采集 Skill 仓库
skillmind ingest https://github.com/anthropics/claude-code-skills

# 3. 提取知识（生成草稿）
skillmind extract

# 4. 审核草稿列表
skillmind review

# 5. 编辑不满意的草稿
skillmind edit <uuid>

# 6. 批量发布到 Obsidian
skillmind publish --all

# 7. 语义搜索
skillmind search "零停机升级"
```

### 增量更新

```bash
# 检查上游是否有更新
skillmind update --check

# 重新采集 + 提取（已有内容自动跳过）
skillmind ingest https://github.com/anthropics/claude-code-skills
skillmind extract

# 查看新增草稿并发布
skillmind review
skillmind publish --all
```

---

## 数据存储位置

| 路径 | 内容 |
|------|------|
| `~/.skillmind/config.yaml` | 全局配置文件 |
| `~/.skillmind/cache/raw/` | 采集的原始 SKILL.md 文件 |
| `~/.skillmind/cache/repos/` | 克隆的 Git 仓库 |
| `~/.skillmind/cache/extract_cache/` | LLM 提取结果缓存（避免重复消费） |
| `~/.skillmind/drafts/` | 草稿 JSON 文件 |
| `~/.skillmind/vault/skills/` | 已发布的 Obsidian Markdown 笔记 |
| `~/.skillmind/vault/_drafts/` | 草稿对应的 Markdown 预览 |
| `~/.skillmind/hashes.yaml` | 文件哈希记录（用于去重） |

> 所有数据均存储在本地，不上传任何云服务。

---

## 支持的 LLM 模型

通过 [litellm](https://github.com/BerriAI/litellm) 统一调用，支持所有兼容接口：

| Provider | 模型示例 | 配置命令 |
|----------|---------|---------|
| Anthropic | `anthropic/claude-3-5-haiku-20241022`、`anthropic/claude-opus-4-5` | `--provider anthropic --api-key sk-ant-xxx` |
| OpenAI | `openai/gpt-4o`、`openai/gpt-4o-mini` | `--provider openai --api-key sk-xxx` |
| DeepSeek | `deepseek/deepseek-chat` | `--provider deepseek --api-key sk-xxx` |
| Google Gemini | `gemini/gemini-1.5-pro` | `--provider gemini --api-key AIza-xxx` |
| Groq | `groq/llama-3.3-70b-versatile` | `--provider groq --api-key gsk_xxx` |
| Mistral | `mistral/mistral-large-latest` | `--provider mistral --api-key xxx` |
| OpenRouter | `openrouter/anthropic/claude-3.5-sonnet` | `--provider openrouter --api-key xxx` |
| 本地模型 | `ollama/llama3` | `--provider openai --api-key ollama --api-base http://localhost:11434` |
| Claude Code Max | 通过 `--auth claude_code_max` 免 Key 使用 | `--auth claude_code_max` |
